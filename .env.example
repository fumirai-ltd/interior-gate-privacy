# Public site URL (no trailing slash). Used for canonical URLs, sitemap, Open Graph, and JSON-LD.
# Set in production builds, e.g. https://www.example.com
NEXT_PUBLIC_SITE_URL=https://www.example.com
